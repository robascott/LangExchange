<%= search_form_for @q, url: "/users/all", class: "user-search-form" do |f| %>

	<%= f.label :username_cont, "Search users" %>
	<%= f.text_field :username_cont, autocomplete: "off" %>

  <button class="search-button">Search</button>

<% end %>