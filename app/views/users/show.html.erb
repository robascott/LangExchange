<h1>Users#show</h1>


<a href="/users/<%= @user['id'] %>/notes/new">Add new note</a>


<ul>

<% @notes.each do |note| %>

	

		<li>
			<%= link_to "#{note['title']}", note_path(note) %>
			<%= link_to 'Delete', user_note_path(@user, note), :method => :delete %>
		</li>


<% end %>


</ul>
